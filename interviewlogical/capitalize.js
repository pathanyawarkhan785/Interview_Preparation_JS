let y = (n) => {
  //   console.log(n);
  //   console.log(n.charAt(0).toUpperCase() + n.slice(1));
};
y('yawar');

let p = (n) => {
  let len = 0;
  //   console.log(n);
  while (n[len] !== undefined) {
    len++;
    return len;
    // console.log(len);
  }
};

// document.write(p("yawar"));
p('yawar');
